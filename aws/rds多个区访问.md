## 问题 #241（纠错后）

一家**在线学习公司**正在迁移到 AWS 云。  
该公司在 **PostgreSQL 数据库**中维护其**学生记录**。  

公司需要一种解决方案，使其数据**始终在两个 AWS 区域可用且保持在线**，并且希望**运营开销最少**。  

哪种解决方案可以满足这些要求？

A. 将 PostgreSQL 数据库迁移到 Amazon EC2 实例上的 PostgreSQL 集群。  
B. 将 PostgreSQL 数据库迁移到**启用了多可用区（Multi-AZ）功能的 Amazon RDS for PostgreSQL** 数据库实例。  
C. 将 PostgreSQL 数据库迁移到 Amazon RDS for PostgreSQL 数据库实例，并在**另一个区域创建一个只读副本**。  
D. 将 PostgreSQL 数据库迁移到 Amazon RDS for PostgreSQL 数据库实例，并设置数据库快照复制到另一个区域。

---

## 正确答案
**👉 C**

---

## 解析

### 考点
- **跨区域（Multi-Region）高可用**
- **数据库需保持在线**
- **最小化运维成本**
- PostgreSQL 在 AWS 上的官方能力边界

---

### 为什么选 C

- **RDS for PostgreSQL 跨区域只读副本**
  - 数据在 **两个区域同时存在**
  - 主区域故障时：
    - 可**手动或自动提升只读副本为主库**
  - 数据始终在线（无离线恢复过程）
- 运维成本：
  - AWS 托管
  - 无需自建复制、监控、故障转移逻辑
- 满足题目所有关键词：
  - ✔ 两个区域
  - ✔ 在线
  - ✔ 最低运营开销（在跨区域前提下）

---

### 其他选项为什么错

#### ❌ A. EC2 上自建 PostgreSQL 集群
- 需要自行管理：
  - 复制
  - 高可用
  - 跨区域同步
- 运维成本 **最高**
- 明显不符合“最少运营开销”

---

#### ❌ B. RDS Multi-AZ
- **Multi-AZ 只在同一 Region 内**
- ❌ 不满足“两个 AWS 区域可用”

---

#### ❌ D. 跨区域快照复制
- 快照是：
  - **离线备份**
  - 灾难恢复用途
- ❌ 无法保证“始终在线”
- 恢复过程慢，RTO 高

---

## 一句话记忆
> **数据库要求“跨区域 + 在线” → RDS 跨区域只读副本**

或者考试版：

> **Multi-AZ ≠ Multi-Region，跨区域只能选 Read Replica**
