## 问题 #344

一家公司有一个 **Java 应用程序**，使用 **Amazon Simple Queue Service（Amazon SQS）** 来处理消息。  
该应用程序当前**无法处理大于 256 KB 的消息**。

公司现在希望：
- 应用程序能够处理 **最大 50 MB 的消息**
- **尽量减少代码更改**

哪种解决方案可以满足这些要求？

A. 使用 **Amazon SQS 扩展客户端库（Java）**，将大于 256 KB 的消息存储在 **Amazon S3** 中。  
B. 使用 **Amazon EventBridge** 从应用程序发布大消息，而不是使用 Amazon SQS。  
C. 修改 Amazon SQS 的限制，使其能够处理大于 256 KB 的消息。  
D. 将大于 256 KB 的消息存储在 **Amazon EFS** 中，并在 SQS 消息中引用该位置。  

---

## 正确答案
**👉 A**

---

## 解析

### 考点
- **Amazon SQS 消息大小限制**
- **SQS Extended Client（扩展客户端）**
- 最小代码改动
- 大消息解耦设计

---

## 为什么选 A

### ✅ SQS 官方支持的大消息解决方案

Amazon SQS **单条消息大小上限是 256 KB（不可更改）**。  
要支持 **50 MB** 的消息，**唯一官方、标准做法**是：

> **SQS Extended Client + Amazon S3**

---

### 1️⃣ SQS 扩展客户端的工作方式

**SQS Extended Client for Java：**
- 自动将：
  - **大于 256 KB 的消息内容存入 S3**
- SQS 中只保留：
  - 一个 **指向 S3 对象的引用（Pointer）**
- 消费端：
  - 自动从 S3 拉取完整消息内容
  - 对应用程序几乎透明

👉 **无需重写业务逻辑**

---

### 2️⃣ 最小代码更改（题目核心）

- 使用的是：
  - **官方 Java SDK 扩展库**
- 仅需：
  - 引入依赖
  - 少量配置
- 不需要：
  - 手动管理 S3
  - 手写引用逻辑

👉 完全符合：
> **“尽量减少代码更改”**

---

### 3️⃣ 支持最大 50 MB（甚至更大）

- 单条消息可达：
  - **S3 对象大小限制**
- SQS 本身仍然只负责：
  - 可靠投递
  - 解耦
  - 重试、可见性超时等能力

---

## 其他选项为什么错

### ❌ B. 使用 Amazon EventBridge
- EventBridge 事件大小限制更小（~256 KB）
- 不适合大消息传递
- ❌ 无法解决 50 MB 问题

---

### ❌ C. 修改 SQS 限制
- SQS 的 256 KB 限制是 **服务级硬限制**
- ❌ 不可配置
- 典型“幻想型选项”

---

### ❌ D. 手动使用 EFS 存储消息
- 需要：
  - 自己管理文件生命周期
  - 自己处理并发与清理
- 代码改动大
- 架构复杂
- ❌ 非最小改动方案

---

## 一句话记忆
> **SQS 要发大消息？答案永远是：S3 + SQS Extended Client**

### 考试秒杀版
> **SQS 超 256 KB → 官方解法只有一个：Extended Client**
