## 问题 #303（纠错后）

一家企业正在启动一个新的应用程序，该应用程序部署在  
**Amazon Elastic Container Service (Amazon ECS)** 集群上，并使用 **Fargate 启动类型**运行 ECS 任务。

公司正在监控 **CPU 和内存使用率**，因为应用程序在启动时会出现高流量。  
同时，公司希望在 **利用率下降时自动降低成本**。

解决方案架构师应该推荐什么？

A. 使用 Amazon EC2 自动扩展，根据以前的流量模式在特定时间段进行扩展。  
B. 使用 AWS Lambda 函数，根据触发 Amazon CloudWatch 警报的指标违规来扩展 Amazon ECS。  
C. 使用 Amazon EC2 自动扩展与简单扩展策略，当 ECS 指标违规触发 Amazon CloudWatch 警报时进行扩展。  
D. 使用 **AWS 应用程序自动扩展（Application Auto Scaling）** 与 **目标跟踪策略**，当 ECS 指标变化时自动扩展或缩减。  

---

## 正确答案
**👉 D**

---

## 解析

### 考点
- **ECS Fargate 的自动扩展方式**
- CPU / 内存驱动的弹性伸缩
- 自动降本（Scale in）
- 托管服务 vs 自定义方案

---

### 为什么选 D

对于 **ECS + Fargate**：

- ❌ 不使用 EC2 Auto Scaling（没有 EC2 实例）
- ❌ 不推荐自写 Lambda 控制扩展
- ✅ **官方推荐：Application Auto Scaling**

**目标跟踪策略（Target Tracking）** 的优势：

- 自动保持目标 CPU / 内存利用率（如 50%）
- 流量高 → 自动扩容
- 流量低 → **自动缩容，立刻省钱**
- 无需人为计算阈值
- 非常适合 **突发流量 + 成本敏感场景**

👉 这是 **ECS Fargate 的标准、最佳实践方案**

---

### 其他选项为什么错

**A. EC2 自动扩展（定时）**
- ❌ 与 Fargate 无关
- ❌ 不能应对实时流量变化
- ❌ 不能自动降本

**B. Lambda + CloudWatch**
- ❌ 可行但 **复杂、非托管**
- ❌ 运维成本高
- ❌ 非考试与生产推荐方案

**C. EC2 自动扩展 + 简单策略**
- ❌ Fargate 不使用 EC2 自动扩展
- ❌ 架构不匹配

---

## 一句话记忆
> **ECS Fargate 自动扩缩容 → Application Auto Scaling + 目标跟踪**
