## 问题 #319

一家公夜在 AWS 云中运行着**大量基于 Linux 的 Amazon EC2 实例**。  
系统管理员目前通过 **共享 SSH 密钥** 来管理这些实例。

在最近的一次安全审计后，安全团队要求：
- **删除所有共享 SSH 密钥**
- 仍需提供对 EC2 实例的**安全访问**
- 并且要 **最小化管理开销**

解决方案架构师应该选择哪种方案？

A. 使用 **AWS Systems Manager Session Manager** 连接到 EC2 实例。  
B. 使用 AWS Security Token Service (AWS STS) 按需生成一次性 SSH 密钥。  
C. 允许共享 SSH 访问一组堡垒实例，并配置所有其他实例仅允许从堡垒实例进行 SSH 访问。  
D. 使用 Amazon Cognito 自定义授权器验证用户身份，并调用 AWS Lambda 函数生成临时 SSH 密钥。

---

## 正确答案
**👉 A**

---

## 解析

### 考点
- **去除 SSH 密钥管理**
- **EC2 安全访问**
- **最小化运维与管理成本**
- AWS 原生运维最佳实践

---

### 为什么选 A

#### ✅ AWS Systems Manager Session Manager

Session Manager 是 **AWS 官方推荐的 EC2 远程访问方式**，优势非常明显：

#### 1️⃣ 完全不需要 SSH
- ❌ 不使用 SSH
- ❌ 不需要密钥
- ❌ 不需要开放 22 端口

👉 直接满足“**删除所有共享 SSH 密钥**”的审计要求。

---

#### 2️⃣ 最小化管理开销
- 使用 **IAM** 控制访问权限
- 无需：
  - 管理密钥生命周期
  - 分发密钥
  - 轮换密钥
- 日志可直接集成：
  - CloudTrail
  - CloudWatch Logs
  - S3

👉 **运维复杂度最低**

---

#### 3️⃣ 安全性极高
- 访问走 AWS API（HTTPS）
- 支持：
  - MFA
  - 细粒度 IAM 策略
- 审计友好（完整会话日志）

---

### 其他选项为什么错

#### ❌ B. STS 生成一次性 SSH 密钥
- AWS **没有原生支持** STS 生成 SSH 密钥
- 需要大量自定义实现
- 运维复杂度高
- ❌ 非最佳实践

---

#### ❌ C. 堡垒机（Bastion Host）
- 仍然需要 SSH 密钥
- 仍需管理：
  - 密钥
  - 堡垒机安全
  - 打补丁
- ❌ 管理开销较大
- 属于**传统方案，不是最优**

---

#### ❌ D. Cognito + Lambda 自定义方案
- 架构复杂
- 维护成本高
- 不符合“**最少管理开销**”要求
- ❌ 过度设计（Over-engineering）

---

## 一句话记忆
> **想要“无 SSH、无密钥、低运维” → 直接上 Session Manager**

考试秒杀版：

> **EC2 运维 + 安全审计 + 不要 SSH = AWS Systems Manager Session Manager**
