## 问题 #243（纠错后）

一家**医学研究实验室**生成与一项新研究相关的数据。  
实验室希望将这些数据以**最低延迟**提供给**全国各地的诊所**，以便在其**本地文件系统应用程序**中使用。  

数据文件存储在一个 **具有只读权限的 Amazon S3 存储桶**中，供多个诊所访问。

解决方案架构师应该推荐哪种方案来满足这些要求？

A. 在每个诊所的本地以虚拟机（VM）方式部署 **AWS Storage Gateway 文件网关**  
B. 使用 **AWS DataSync** 将文件迁移到每个诊所的本地应用程序进行处理  
C. 在每个诊所的本地以虚拟机（VM）方式部署 **AWS Storage Gateway 卷网关**  
D. 将 **Amazon EFS** 文件系统附加到每个诊所的本地服务器

---

## 正确答案
**👉 A**

---

## 解析

### 考点
- **S3 数据 → 本地文件系统访问**
- **最低访问延迟**
- **只读共享数据**
- AWS 混合存储解决方案的正确选择

---

### 为什么选 A（Storage Gateway 文件网关）

**AWS Storage Gateway 文件网关特性：**

- 将 **Amazon S3 映射为本地文件系统（NFS / SMB）**
- 支持：
  - 本地应用程序无改动访问
  - **本地缓存 → 低延迟**
- 非常适合：
  - 多站点
  - 只读数据
  - 分发型访问场景

👉 完美匹配题目关键词：
- S3 是主存储
- 诊所使用本地文件系统应用
- 要求最低延迟
- 只读权限

---

### 其他选项为什么错

#### ❌ B. AWS DataSync
- DataSync 用于：
  - 一次性或周期性迁移
- ❌ 不适合持续、实时访问
- 会产生多份数据副本
- 无法保证最低延迟更新

---

#### ❌ C. Storage Gateway 卷网关
- 卷网关提供：
  - **块存储（iSCSI）**
- ❌ 本题需要 **文件级访问**
- 与 S3 文件共享场景不匹配

---

#### ❌ D. Amazon EFS
- EFS 只能挂载到：
  - **VPC 内的 EC2**
- ❌ 无法直接附加到本地服务器
- 不支持跨 Internet 本地访问

---

## 一句话记忆
> **S3 数据要以文件系统方式、低延迟给本地使用 → Storage Gateway 文件网关**

考试口诀版：

> **S3 ↔ 本地文件系统 → 只想 File Gateway**
