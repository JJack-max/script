## 问题 #351

一家公司正在将其**数据管理应用程序**迁移到 **AWS**。  
公司希望在迁移过程中：

- 过渡到**事件驱动架构**
- 架构需要是**分布式的**
- 在工作流的不同阶段使用**无服务器（Serverless）**服务
- **尽量减少运营开销**

哪种解决方案能够**最好地满足这些要求**？

A. 在 **AWS Glue** 中构建工作流。使用 AWS Glue 调用 AWS Lambda 函数来处理工作流步骤。  
B. 在 **AWS Step Functions** 中构建工作流。在 **Amazon EC2 实例**上部署应用程序，并使用 Step Functions 调用 EC2 上的工作流步骤。  
C. 在 **Amazon EventBridge** 中构建工作流。使用 EventBridge 按计划调用 AWS Lambda 函数来处理工作流步骤。  
D. 在 **AWS Step Functions** 中构建工作流。使用 **状态机** 调用 **AWS Lambda** 函数来处理工作流步骤。

---

## 正确答案
**👉 D**

---

## 解析

### 考点
- **事件驱动架构**
- **Serverless 工作流编排**
- 分布式系统设计
- **最低运营开销的 AWS 原生方案**

---

## 为什么选 D

### ✅ Step Functions + Lambda = 官方无服务器工作流方案

**方案 D 完全符合题目中的所有关键词：**
> 事件驱动、分布式、无服务器、低运维

---

### 1️⃣ Step Functions 负责“流程编排”

- Step Functions 提供：
  - 可视化工作流
  - 状态管理
  - 重试、回滚、并行执行
- 天然适合：
  - **多步骤工作流**
  - **分布式任务编排**

👉 不需要自己维护流程状态

---

### 2️⃣ Lambda 负责“具体执行逻辑”

- 每个工作流步骤：
  - 由一个 Lambda 函数处理
- 完全 **Serverless**
  - 无服务器管理
  - 自动扩缩
  - 按调用计费

👉 极低运营开销

---

### 3️⃣ 事件驱动 & 解耦架构

- Step Functions 可以：
  - 被 EventBridge、API Gateway、S3 事件等触发
- 各步骤松耦合
- 非常适合事件驱动设计

---

### 4️⃣ 运维成本最低

- 无 EC2
- 无集群
- 无补丁
- 无容量规划

👉 **AWS 考试中“低运维 + 工作流”几乎必选 Step Functions + Lambda**

---

## 其他选项为什么错

### ❌ A. AWS Glue 工作流
- Glue 更适合：
  - ETL
  - 数据湖处理
- 不适合通用应用工作流
- 服务较重，运维复杂度更高

---

### ❌ B. Step Functions + EC2
- EC2：
  - 需要维护
  - 需要扩缩容
- ❌ 违反“无服务器 + 低运营开销”的要求

---

### ❌ C. EventBridge 直接编排工作流
- EventBridge：
  - 擅长事件路由
  - **不擅长复杂多步骤工作流**
- 缺少：
  - 状态管理
  - 错误处理
  - 流程控制

---

## 一句话记忆
> **无服务器事件驱动工作流 = Step Functions + Lambda**

### 考试秒杀版
> **看到“分布式 + 工作流 + Serverless” → 直接选 Step Functions + Lambda**
