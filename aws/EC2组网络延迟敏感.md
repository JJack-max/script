## 问题 #306（纠错后）

一家企业希望为运行在 **Amazon EC2 实例** 上的**延迟敏感型应用程序**运行一个**内存数据库**。  
该应用程序具有以下特征：

- **每分钟处理超过 100,000 个请求**
- 需要 **极高的网络吞吐量**
- 对 **网络延迟极其敏感**
- 希望采用 **最具成本效益** 的网络设计
- 需要 **尽量减少数据传输费用**

解决方案架构师应推荐哪种方案？

A. 在同一个 AWS 区域内的**同一个可用区**启动所有 EC2 实例。启动 EC2 实例时指定具有**集群策略的放置组**。  
B. 在同一个 AWS 区域内的**不同可用区**启动所有 EC2 实例。启动 EC2 实例时指定具有**分区策略的放置组**。  
C. 部署一个自动扩展组，根据网络利用率目标在**不同可用区**启动 EC2 实例。  
D. 部署一个具有步进扩展策略的自动扩展组，在**不同可用区**启动 EC2 实例。  

---

## 正确答案
**👉 A**

---

## 解析

### 考点
- **超低延迟 + 超高网络吞吐**
- EC2 **放置组（Placement Group）** 选型
- **跨 AZ 数据传输成本**
- 内存数据库架构设计

---

### 为什么选 A

这是一个**教科书级的「Cluster Placement Group」场景**。

#### ✅ 集群放置组（Cluster Placement Group）的特性
- EC2 实例**物理上尽可能靠近**
- 提供：
  - **最低网络延迟**
  - **最高网络吞吐量**
- 非常适合：
  - 内存数据库
  - 高频交易
  - 分布式缓存
  - 实时分析系统

#### ✅ 同一可用区的优势
- **AZ 内流量不收费**
- 避免跨 AZ 网络费用
- 延迟最低

#### ✅ 成本最优
- 无跨 AZ 数据传输费用
- 不需要复杂的跨 AZ 同步机制

👉 **延迟敏感 + 内存数据库 + 高吞吐 = 同 AZ + Cluster Placement Group**

---

### 其他选项为什么错

**B. 不同 AZ + 分区放置组**
- ❌ 分区策略用于 **故障隔离**
- ❌ 网络延迟高于集群策略
- ❌ 跨 AZ 数据传输 **有费用**
- ❌ 不适合延迟敏感场景

**C. 不同 AZ 的自动扩展组**
- ❌ 目标是高可用，不是低延迟
- ❌ 网络路径更长
- ❌ 成本更高

**D. 步进扩展 + 不同 AZ**
- ❌ 与低延迟目标冲突
- ❌ 引入不必要的复杂性和成本

---

## 一句话记忆
> **极低延迟 + 极高吞吐 + 省网络费 → 同 AZ + Cluster Placement Group**
