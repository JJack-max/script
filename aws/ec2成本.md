## 问题 #238（纠错后）

一家公司希望**允许其工程师团队在多个独立的 AWS 账户中进行实验**。  
公司希望在**给定月份内**，当**任一账户的 Amazon EC2 实例使用量达到特定阈值时立即收到通知**。

解决方案架构师应该如何**以最具成本效益的方式**满足该要求？

A. 使用 **Cost Explorer（成本探查器）** 创建按服务划分的**每日**成本报告，并按 EC2 实例过滤。当超过阈值时，通过 Amazon SES 发送通知。  
B. 使用 **Cost Explorer** 创建按服务划分的**每月**成本报告，并按 EC2 实例过滤。当超过阈值时，通过 Amazon SES 发送通知。  
C. 使用 **AWS Budgets** 为**每个账户**创建**每月成本预算**，将范围限定为 **EC2 实例**，设置预算警报阈值，并通过 **Amazon SNS** 在超出阈值时发送通知。  
D. 使用 **AWS Cost and Usage Report（CUR）** 创建**小时粒度**报告，将数据与 Amazon Athena 集成，使用 EventBridge 定期查询，并通过 Amazon SNS 发送通知。

---

## 正确答案
**👉 C**

---

## 解析

### 考点
- **跨多个 AWS 账户的成本监控**
- **按服务（EC2）设置阈值**
- **实时 / 近实时通知**
- **成本效益最优方案**

---

### 为什么选 C

- **AWS Budgets 是官方的阈值告警工具**
- 支持：
  - 按 **账户**
  - 按 **服务（EC2）**
  - 按 **月度周期**
- 可直接：
  - 设置预算阈值
  - 超出阈值立即触发 **SNS 通知**
- **无需额外查询、存储或计算成本**
- 配置简单、维护成本最低

👉 **这是 AWS 官方推荐的标准做法**

---

### 其他选项为什么错

#### ❌ A / B. Cost Explorer + SES
- Cost Explorer 主要用于：
  - 分析
  - 可视化
- ❌ 不适合实时阈值告警
- ❌ SES 不是成本告警的常规集成方式
- 成本和运维复杂度都高于 Budgets

---

#### ❌ D. CUR + Athena + EventBridge
- 架构复杂
- 运维成本高
- 适合：
  - 深度成本分析
  - 离线报表
- ❌ **严重过度设计（over-engineering）**

---

## 一句话记忆
> **AWS 成本阈值告警 = AWS Budgets + SNS**

或者更直接：

> **看到“成本 + 阈值 + 通知” → 直接选 AWS Budgets**
